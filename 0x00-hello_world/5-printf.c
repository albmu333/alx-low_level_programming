#include <stdio.h>
/**
 * main - 5-program
 * description - write a C program that prints ecactly
 *
 * Return: always (0) (success);
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
